version: '2'
services:
  app:
    container_name: easy-homeworks-front-dev
    build:
      context: ./
      dockerfile: ./Dockerfile.dev
    ports:
      - 3000:3000
    volumes:
      - ./src:/app/src
      - ./public:/app/public
