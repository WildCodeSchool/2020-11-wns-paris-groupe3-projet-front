version: '2'
services:
  app:
    container_name: easy-homeworks-front-prod
    build:
      context: ./
      dockerfile: ./Dockerfile.prod
    ports:
      - 3000:3000
    volumes:
      - ./src:/app/src
      - ./public:/app/public
